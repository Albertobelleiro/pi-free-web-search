---
description: Balanced web research and documentation lookup with free_web_search
---
Research this topic using the `free_web_search` and `free_fetch_content` tools:

$@

Budget profile: **balanced default**

Requirements:
- Prioritize official documentation, primary sources, and exact docs pages.
- Start with `free_web_search({ query, detail: "lean" })`.
- Use `includeContent: true` when a small multi-source synthesis improves answer quality.
- If needed, use `free_fetch_content` on the strongest 1-3 results to study the source directly.
- Keep `free_fetch_content` in summary mode unless exact wording or full structure matters.
- Analyze before answering.
- Return a concise, evidence-backed answer with exact links, caveats, and any uncertainty.
