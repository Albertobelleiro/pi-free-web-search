---
description: Research the web and study exact documentation with free_web_search
---
Research this topic using the `free_web_search` and `free_fetch_content` tools:

$@

Requirements:
- Prioritize official documentation, primary sources, and exact docs pages.
- Use `free_web_search` first; use `includeContent=true` when the exact page content matters.
- If needed, use `free_fetch_content` on the strongest results to study the source directly.
- Analyze before answering.
- Return a concise, evidence-backed answer with exact links, caveats, and any uncertainty.
